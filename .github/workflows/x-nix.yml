name: nix

on:
  push:
    branches:
      - main
    paths:
      - nix/**
  pull_request:
    branches:
      - main
    paths:
      - nix/**
      - .github/workflows/x-nix.yml
      - .github/workflows/x-lib.yml

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: read

jobs:
  x:
    uses: ./.github/workflows/x-lib.yml
    with:
      working-directory: nix
    secrets:
      GH_CACHIX: ${{ secrets.GH_CACHIX }}
